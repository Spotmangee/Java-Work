/*
package Final;

import org.junit.Assert;
import org.junit.Test;

/**
 * Created by Karstan on 07/05/2017.
 *//*
public class decryptorTest {

    @Test
    public void testObjects() {

        Decryptor decrypt = new Decryptor();

        //define stuff here
        String serverMessage  = decrypt.getServerMessage();
        byte[] ciphertext = decrypt.getCiphertext();
        byte[] messageChar = decrypt.getMessageChar();

        String expected = serverMessage;
        byte[] expected2 = ciphertext;
        byte[] expected3 = messageChar;

        Assert.assertEquals(expected, serverMessage);
        Assert.assertEquals(expected2, ciphertext);
        Assert.assertEquals(expected3, messageChar);

        //fail because class not defined properly?
    }
}
*/