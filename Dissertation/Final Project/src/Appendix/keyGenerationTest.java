/*
package Final;

import org.junit.Assert;
import org.junit.Test;

import javax.crypto.SecretKey;
import javax.crypto.spec.IvParameterSpec;
import javax.crypto.spec.SecretKeySpec;
import java.io.UnsupportedEncodingException;
import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;
import java.util.Arrays;

/**
 * Created by Karstan on 07/05/2017.
 */
/*
public class keyGenerationTest {

    @Test
    public void testObjects() {

        keyGeneration key = new keyGeneration();

        SecretKey key1 = key.getKey1();
        SecretKey key2 = key.getKey2();
        IvParameterSpec ivSpec = key.getIvSpec();

        SecretKey expected = key1;
        SecretKey expected2 = key2;
        IvParameterSpec expected3 = ivSpec;

        Assert.assertSame(expected, key1);
        Assert.assertSame(expected2, key2);
        Assert.assertSame(expected3, ivSpec);

        //fail because class not defined properly?
    }
}
*/
